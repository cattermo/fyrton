name: Deploy
on:
  push:
    branches: [ master ]

jobs:
  deploy:
    name: Build frontend + public-go
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 12
    - run: npm ci
    - run: npm run generate
    - run: node ./deploy.js
